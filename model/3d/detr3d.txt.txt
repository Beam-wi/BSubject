一、先理解detr
    英文名 detection transformer (就是注意力机制目标检测)

    思路是：
        1. 先通过卷积做特征提取
        2. 然后通过transform 的encode和decode预测 N 个预测值集合
        3. bipartite loss(集合之间的loss) 将预测的N个预测值集合与M(数小于N时用空集合补充到N)个真实值做匹配，找到最优匹配做损失回归，相当给每个真实框找最优匹配
        4. 然后通过卷积将 预测集合 和 真实集合 对应到 坐标 和 置信度 求损失

